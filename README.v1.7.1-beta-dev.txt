ChatGPT Conversation Crawler v1.7.1-beta-dev

Development diagnostic prerelease.

This build replaces stale-pixel target recovery with logical retained-turn navigation, processes retained turns one by one with whole-turn viewport coverage and turn-scoped disclosure expansion, performs reverse/forward semantic verification with targeted reconciliation, and separates canonical real hydration generations from synthetic preservation unions.

It also reports turn-local convergence failures through centralized archive integrity, gives diagnostic snapshots a truthful detached integrity result, installs Chromium in CI for existing browser-backed tests, and bounds the existing server integration test teardown.

Run the platform setup script once, then use the matching start script. The development diagnostic wrapper remains enabled in this prerelease and must not be merged into clean main.
