ChatGPT Conversation Crawler v1.7.0-beta3-dev

Development diagnostic prerelease.

This build completes the v1.7 diagnostic/cleanup batch: truthful MHTML progress fields, mounted-vs-retained turn counts, bounded/coalesced MHTML capture, shared core navigation, launcher parity, and readable maintained source.

Run the platform setup script once, then use the matching start script. The development diagnostic wrapper remains enabled in this prerelease.
